<div class='flex-column c-following d-none c-unselected-flw'>
  <% @users2.each do |j| %>
   <div class='d-flex my-2 flex-column flex-md-row'>
     <%= link_user_img(j) %>
     <div class='d-flex flex-column w-100 justify-content-center'>
         <span class='font-weight-bold text-dark text-center'>
           <%= j.full_name %>
         </span>
       <span class='text-center'>@<%= j.username %></span>
      </div>
       <span class='mx-2 font-weight-bold align-self-center'>
         <%= btn_follow(j) %>
       </span>
   </div>
  <% end %> 
</div>